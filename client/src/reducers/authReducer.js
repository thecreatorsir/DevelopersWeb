const initialstate = {
  isAuthenticated: false,
  user: {},
};

const authReducer = (state = initialstate, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default authReducer;
